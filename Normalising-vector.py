Import numpy as np

xn = x/np.linalg.norm(x)

#np.linalg.norm(argument) finds the norm of the vector. Use 2 arguments for type of norm as well as what to find the norm of
#vector/vector's norm = normalised vector, making it a unit vector
